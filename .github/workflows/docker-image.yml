name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  set-env:
    runs-on: ubuntu-latest
    outputs:
      short_sha: ${{ steps.short-sha.outputs.sha }}
    steps:
    - name: Set Short SHA as Output
      id: short-sha
      run: echo "::set-output name=sha::$(echo ${{ github.sha }} | cut -c1-7)"

  build:
    needs: set-env
    runs-on: ubuntu-latest
    steps:
    - name: Checkout the repository
      uses: actions/checkout@v2
    - name: Build the Docker image
      env:
        SHORT_SHA: ${{ needs.set-env.outputs.short_sha }}
      run: docker build . --file Dockerfile --tag k8s-app:${{ env.SHORT_SHA }}

  push:
    needs: build
    runs-on: ubuntu-latest
    steps:
    - name: Log in to GitHub Container Registry
      env:
        SHORT_SHA: ${{ needs.set-env.outputs.short_sha }}
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
    - name: Push the Docker image
      env:
        SHORT_SHA: ${{ needs.set-env.outputs.short_sha }}
      run: docker tag k8s-app:${{ env.SHORT_SHA }} ghcr.io/${{ github.repository }}:latest
